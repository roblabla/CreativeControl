package me.FurH.CreativeControl.integration;

import org.bukkit.entity.*;
//import com.cypherx.xauth.*;
import org.bukkit.*;

public class xAuth
{
    public static boolean isLoggedIn(final Player player) {
    /*    final xAuthPlayer xpl = getAuth().getPlayerManager().getPlayer(player.getName());
        boolean li = true;
        if (!xpl.isAuthenticated()) {
            li = false;
        }
        else if (xpl.isGuest()) {
            li = false;
        }
        return li;*/
        return false;
    }
    
/*    private static com.cypherx.xauth.xAuth getAuth() {
        return (com.cypherx.xauth.xAuth)Bukkit.getServer().getPluginManager().getPlugin("xAuth");
    }*/
}
